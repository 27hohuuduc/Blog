{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Server",
      "program": "${workspaceFolder}/src/StartUp.ts",
      "preLaunchTask": "tsc: build - tsconfig.dev.json",
      "outFiles": [
        "${workspaceFolder}/.dev/**/*.js"
      ]
    }
  ]
}